class xTeam {
    constructor(name, data) {
        this.name = name
        this.enabled = data.used,
        this.spawnpoint = data.spawnpoint
        this.active = 0
        this.blip = data.blip
    }
}